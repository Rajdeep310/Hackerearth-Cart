import React from 'react'

function SingleItem(item) {
    const { name, price,img_url, id } = item;
    return (
        <div>
            SingleItem
        </div>
    )
}

export default SingleItem
